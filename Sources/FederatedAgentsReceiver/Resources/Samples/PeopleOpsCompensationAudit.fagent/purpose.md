# Purpose

The sender wants a quick compensation parity check over the receiver's local HR export.

The goal is to compare compensation distributions across department, level, and location without exposing employee-level records.

The sender does not need raw rows, named examples, or identifiers.
